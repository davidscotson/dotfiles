# WARNING: Do not edit this file.
# It was generated by processing {{ yadm.source }}
# for user {{ yadm.user }}
#
[core]
    pager = delta
    editor = nvim
#    fsmonitor = .git/hooks/fsmonitor-watchman
[user]
    name = David Scotson
{% if yadm.user == "david.scotson" %}
    email = david.scotson@synergy-learning.com
{% else %}
    email = david.scotson@gmail.com
{% endif %}
[merge]
    ff = only
    conflictstyle = zdiff3
    tool = nvimdiff1
[mergetool]
  prompt = false
[pull]
    ff = only
[difftool]
    prompt = true
[diff]
    colorMoved = default
    tool = nvimdiff
[init]
    defaultBranch = main
[interactive]
    diffFilter = delta --color-only
[delta]
    navigate = true
    #syntax-theme = 'Monokai Extended Origin'
    side-by-side = true
    dark = true
    hyperlinks = true
